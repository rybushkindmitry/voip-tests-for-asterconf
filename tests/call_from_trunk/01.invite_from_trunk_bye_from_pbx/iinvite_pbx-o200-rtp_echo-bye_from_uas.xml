<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!-- UAS: АТС принимает звонок от транка, медиа отражается флагом -rtp_echo
     (аргументы задачи sipp_uas_pbx_rtp_bye в config.yml). BYE инициирует АТС.
     INVITE -> 200 (SDP) -> ACK -> пауза -> BYE -> 200. -->
<scenario name="iinvite_pbx-o200-rtp_echo-bye_from_uas">

  <recv request="INVITE" rrs="true" start_txn="invite"/>

  <send>
    <![CDATA[
      SIP/2.0 200 OK
      [last_Via:]
      [last_Record-Route:]
      [last_From:]
      [last_To:];tag=[pid]SIPpTag01[call_number]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: sip:[service]@[local_ip]:[local_port]
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 8
      a=rtpmap:8 PCMA/8000

    ]]>
  </send>

  <!-- тег транка живёт в From входящих INVITE/ACK: peer_tag_param у UAS пуст
       (он берётся из To полученных ответов), поэтому достаём через ereg -->
  <recv request="ACK" crlf="true">
    <action>
      <ereg regexp="tag=[^;\r\n]*" search_in="hdr" header="From:"
            check_it="true" assign_to="trunk_tag"/>
    </action>
  </recv>

  <pause milliseconds="2000"/>

  <send retrans="500" start_txn="bye">
    <![CDATA[
      BYE [next_url] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      Route: <sip:[remote_ip]:[remote_port];lr>
      From: sip:[tel]@[local_ip]:[local_port];tag=[pid]SIPpTag01[call_number]
      To: sip:[service]@[remote_ip]:[remote_port];[$trunk_tag]
      Call-ID: [call_id]
      CSeq: [cseq] BYE
      Contact: sip:[service]@[local_ip]:[local_port]
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" response_txn="bye"/>

</scenario>
